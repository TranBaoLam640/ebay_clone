# Keeps MongoDB scheduled/alive ahead of surge & HPA backend pods on the single node.
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: mongodb-critical
value: 1000000
globalDefault: false
description: "Stateful MongoDB — must outrank stateless backend surge/HPA pods."
