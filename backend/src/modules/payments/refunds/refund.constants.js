export const REFUND_STATUSES = Object.freeze([
  'PROCESSING',
  'COMPLETED',
  'FAILED',
]);

export const REFUND_SOURCE_TYPES = Object.freeze(['INR']);

export const REFUND_METHODS = Object.freeze(['COD', 'PAYPAL']);
